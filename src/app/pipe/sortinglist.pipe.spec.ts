import { SortinglistPipe } from './sortinglist.pipe';

describe('SortinglistPipe', () => {
  it('create an instance', () => {
    const pipe = new SortinglistPipe();
    expect(pipe).toBeTruthy();
  });
});
