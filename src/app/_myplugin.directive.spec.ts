import { MypluginDirective } from './myplugin.directive';

describe('MypluginDirective', () => {
  it('should create an instance', () => {
    const directive = new MypluginDirective();
    expect(directive).toBeTruthy();
  });
});
